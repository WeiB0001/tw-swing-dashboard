# -*- coding: utf-8 -*-
"""
config.py — 全專案唯一的參數設定檔

之後你要用 Claude 調整訊號邏輯，90% 的情況只需要改這個檔案。
每個參數都寫清楚「改大會怎樣、改小會怎樣」。
"""

from zoneinfo import ZoneInfo

# ---------------------------------------------------------------------------
# 基本設定
# ---------------------------------------------------------------------------
TZ = ZoneInfo("Asia/Taipei")          # 全程使用台北時間
HISTORY_PERIOD = "10mo"                # 向 yfinance 要多久的日線（要 > 20 日均量 + RSI 暖身期）
MIN_BARS = 40                          # 少於這個根數的個股直接跳過（資料不足算不準）

# ---------------------------------------------------------------------------
# 掃描池（Universe）：要掃哪些股票
# ---------------------------------------------------------------------------
TOP_N_BY_TURNOVER = 100    # 依「當日成交金額」取前 N 名（流動性最好的）
MAX_UNIVERSE = 150         # 加上權值股後的總上限，避免 yfinance 抓太久

# 流動性門檻：不符合的直接踢掉，避免掃到冷門股／雞蛋水餃股
MIN_CLOSE_PRICE = 10.0          # 股價低於 10 元不看（跳動一檔就是好幾 %）
MIN_TURNOVER_TWD = 50_000_000   # 當日成交金額低於 5,000 萬不看

# 主要權值股白名單：即使當天成交金額沒進前 100 也一定納入掃描
CORE_WEIGHTED_STOCKS = [
    "2330", "2317", "2454", "2308", "2382", "2412", "2881", "2882", "2891",
    "2886", "2884", "2885", "2892", "2880", "2887", "2883", "5880", "2801",
    "1301", "1303", "1326", "6505", "1216", "1101", "2002", "2207", "2105",
    "2603", "2609", "2615", "2610", "3711", "2303", "3231", "3034", "2379",
    "2357", "3008", "2409", "2408", "4938", "2327", "2345", "3661", "3037",
    "2474", "1590", "2395", "6669", "3017", "2377", "2376", "5871", "9910",
    "2912", "1402", "2049", "6415", "8046", "3045", "4904", "2356", "2301",
]

# ---------------------------------------------------------------------------
# 技術指標參數
# ---------------------------------------------------------------------------
RSI_PERIOD = 14
MA_SHORT = 5
MA_MID = 10
MA_LONG = 20
VOL_MA_PERIOD = 20     # 幾日均量
RANGE_SHORT = 5        # 近 N 日高低點
RANGE_LONG = 20        # 近 N 日高低點

# ---------------------------------------------------------------------------
# 訊號門檻（想讓訊號更嚴格 → 調低 RSI_OVERSOLD、調高 VOL_SURGE_RATIO）
# ---------------------------------------------------------------------------
RSI_OVERSOLD = 35.0        # RSI 低於此值視為超賣
RSI_DEEP_OVERSOLD = 22.0   # 極度超賣，給滿分
RSI_MAX_INTEREST = 52.0    # RSI 高於此值視為「不算超賣」，該項 0 分
VOL_SURGE_RATIO = 1.3      # 量能倍數達此值才開始明顯加分
VOL_FULL_RATIO = 2.2       # 量能倍數達此值該項給滿分
VOL_BLOWOFF_RATIO = 5.0    # 超過此倍數視為異常爆量（可能是利空出貨），扣分
NEAR_LOW_ZONE = 0.40       # 收盤價落在近 20 日區間的下緣 40% 內，才算「靠近低點」

# ---------------------------------------------------------------------------
# 分數權重（五項加總 = 100 分）
# 想讓「量能」更重要 → 把 volume 調高、其他調低，維持總和 100 即可。
# ---------------------------------------------------------------------------
WEIGHTS = {
    "oversold":   30,   # 超賣反彈：RSI 低 + 收盤站上 MA5/MA10
    "volume":     20,   # 量能確認：成交量 / 20 日均量
    "near_low":   20,   # 位階：靠近近 20 日低點且開始反彈
    "ma_reclaim": 20,   # 均線：金叉或剛站上短均線
    "structure":  10,   # 結構品質：MA20 斜率、回檔深度是否合理
}

# ---------------------------------------------------------------------------
# 風險扣分（乘數，1.0 = 不扣）
# ---------------------------------------------------------------------------
CHASE_HIGH_PCT = 8.0       # 當日漲幅超過此 % 視為追高（接近漲停），打折
CHASE_HIGH_PENALTY = 0.78
BLOWOFF_PENALTY = 0.85     # 異常爆量的折扣
BELOW_ALL_MA_PENALTY = 0.7 # 收盤仍在 MA5、MA10 之下（完全沒反彈跡象）

# ---------------------------------------------------------------------------
# 輸出設定
# ---------------------------------------------------------------------------
TOP_N_DISPLAY = 18         # 網頁上顯示前幾名（規格建議 15～20）
MIN_SCORE_TO_SHOW = 25     # 分數低於此值即使進前 18 名也不顯示（寧缺勿濫）

OUTPUT_HTML = "index.html"
OUTPUT_JSON = "data/latest.json"
ARCHIVE_DIR = "data/history"   # 每日結果存檔，之後可做回測

# ---------------------------------------------------------------------------
# 資料來源
# ---------------------------------------------------------------------------
TWSE_STOCK_DAY_ALL = "https://openapi.twse.com.tw/v1/exchangeReport/STOCK_DAY_ALL"
TWSE_INDEX_HIST = "https://openapi.twse.com.tw/v1/indicesReport/MI_5MINS_HIST"
TWSE_MARKET_INDEX = "https://openapi.twse.com.tw/v1/exchangeReport/MI_INDEX"
FINMIND_API = "https://api.finmindtrade.com/api/v4/data"   # 選用，需 FINMIND_TOKEN 環境變數

HTTP_TIMEOUT = 25
YF_CHUNK_SIZE = 40     # yfinance 一次抓幾檔（太大容易被限流）
YF_RETRY = 3
